// The health page is a client component, so its tab title lives here.
export const metadata = { title: "Health" };

export default function HealthLayout({ children }: { children: React.ReactNode }) {
  return children;
}
