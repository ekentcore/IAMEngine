## Tableau (`tableau`)

`Module: none (manual)` · `Mode: manual` · `Build tier: 3` · `Appears in: <1%` · `Lanes: onboard`

Analytics licensing handled by an application champion, not a self-service API path here.

### Onboard lane
`on-request`. Reach out to the application champion for licensing if the user needs Tableau.

### Config keys
`champion`.

### Functions
None — surfaced as a manual checklist item with the champion contact.

### Depends on
identity.

### Variants & gotchas
Champion-driven; not worth API automation at current volume.

### Manual fallback
This module is manual by design.
