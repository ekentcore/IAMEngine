"""kbgen — the Phase 6 KB extraction stage.

Parses ServiceNow KB runbook HTML into the profile-generator IR (see ../ir.schema.json).
"""
__all__ = ["catalog", "sectioning", "families", "registry", "loader"]
